#include <stdio.h>
int main (){
	int a = -1;
	// kiem tra a la duong hay am
	if (a > 0){
		printf("%d la so duong",a);
	}else if(a == 0) {
		printf("a = 0",a);
	} else {
		printf("%d la so am",a);
		
	}
	getchar();
	return 0;
}
 		
